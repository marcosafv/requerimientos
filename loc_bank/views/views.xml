<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="loc_bank.list">
      <field name="name">loc_bank list</field>
      <field name="model">loc_bank.loc_bank</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="loc_bank.action_window">
      <field name="name">loc_bank window</field>
      <field name="res_model">loc_bank.loc_bank</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="loc_bank.action_server">
      <field name="name">loc_bank server</field>
      <field name="model_id" ref="model_loc_bank_loc_bank"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="loc_bank" id="loc_bank.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="loc_bank.menu_1" parent="loc_bank.menu_root"/>
    <menuitem name="Menu 2" id="loc_bank.menu_2" parent="loc_bank.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="loc_bank.menu_1_list" parent="loc_bank.menu_1"
              action="loc_bank.action_window"/>
    <menuitem name="Server to list" id="loc_bank" parent="loc_bank.menu_2"
              action="loc_bank.action_server"/>
    -->
  </data>
</openerp>